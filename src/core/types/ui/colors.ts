import { hexadecimal } from "../basics";

type HexColor3 = `${hexadecimal}${hexadecimal}${hexadecimal}`;
//TODO: Create the complete color type
//! This type produces an error of: Too complex type
// type HexColor6 = `${HexColor3}${HexColor3}`;
